const obj = {
    name: "Harsh",
    age: 19,
    city: "Mumbai"
}

const entries = Object.entries(obj);
console.log(entries);

const keys = Object.keys(obj);
console.log(keys);

const values = Object.values(obj);
console.log(values);